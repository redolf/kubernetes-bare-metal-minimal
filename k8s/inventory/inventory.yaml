all:
    children:
        k8s:
            hosts:
                k8s-c1.{{ host_domain }}:
                k8s-w1.{{ host_domain }}:
                k8s-w2.{{ host_domain }}:
        k8s_master:
            hosts:
                k8s-c1.{{ host_domain }}:
